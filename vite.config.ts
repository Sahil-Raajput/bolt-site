import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // Plain HTML/CSS/JS project — no React plugin needed.
  // Vite serves index.html from the project root as the entry point.
});
